/* sampleCodeModule.c */
 #include <lib2.h>

int main() {

	char * m = "mundo";
	//printf("a ver si esto anda");
	printf("hola %s %d", m, 10);
   /*
	while(1) {
		unsigned char a = getChar();
		if(a != 0)
			putChar(a); 
	}
	return 0xDEADBEEF;
	*/
	return 0;
}